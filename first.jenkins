#!groovy

properties([disableConcurrentBuilds()])

pipeline {
    agent { 
        any
        }
    stages {
        stage("First step") {
            steps{
                sh 'echo \'Hello World\''
            }
        }
    }
}
