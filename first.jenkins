#!groovy

properties([disableConcurrentBuilds()])

pipeline {
    agent { 
        label 'main'
        }
    stages {
        stage("First step") {
            steps{
                sh 'echo \'Hello World\''
            }
        }
    }
}
