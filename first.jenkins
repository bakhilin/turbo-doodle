#!groovy

properties([disableConcurrentBuilds()])

pipeline {
    agent { 
        
        }
    stages {
        stage("First step") {
            steps{
                sh 'echo \'Hello World\''
            }
        }
    }
}
